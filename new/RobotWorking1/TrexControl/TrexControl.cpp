/*
  TrexControl.cpp is a Library for controlling a Trex DMC01
  motor controller through the serial port at the default
  baud rate of 19200 written for teensy 3.0 with teensyduino
 */
 
 #include "TrexControl.h"

/*
      Set up to work with Serial1 on teensy 
      note- replace all Serial1 with Serial
      to work for most arduinos 
*/
   TrexControl::TrexControl(void){
   }
   
   void TrexControl::goStraight(int speed)}
   }
   
   void TrexControl::fullForward(){
   }
   
   void TrexControl::fullStop(){
   }
   
   void TrexControl::slowDown(){
   }
   
   void TrexControl::left(){
   }
   
   void TrexControl::right(){
   }
   
   void TrexControl::reverse(){
   }
   
   void TrexControl::rotate(){
   }
    
   void TrexControl::sendMotorCommand(unsigned char motorCommand, unsigned char value){
   }
    
   unsigned char TrexControl::getData(unsigned char dataCommand){
   }
      


